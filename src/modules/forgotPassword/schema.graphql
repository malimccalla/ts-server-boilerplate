type Mutation {
  sendForgotPasswordEmail(email: String!): ForgotPasswordResponse!
  forgotPasswordChange(newPassword: String!, key: String!): ForgotPasswordResponse!
}

type ForgotPasswordResponse {
  ok: Boolean!
  error: Error
}
